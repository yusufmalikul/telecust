# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELE_BOT_TOKEN=your-telegram-bot-token-here
# Alternative variable name (either one works)
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Use alternative OpenAI-compatible API
# OPENAI_API_BASE=https://api.openai.com/v1
# For example, ChatAnywhere:
# OPENAI_API_BASE=https://api.chatanywhere.org/v1

# Optional: Specify the model to use (default: gpt-3.5-turbo)
# For ChatAnywhere, use: gpt-3.5-turbo-ca, gpt-4o-ca, etc.
# For official OpenAI, use: gpt-3.5-turbo, gpt-4, gpt-4o, etc.
# OPENAI_MODEL=gpt-3.5-turbo-ca

# Optional: Number of recent messages to include in conversation context (default: 10)
# Lower values = less context but faster/cheaper, Higher values = more context but slower/costlier
# CONVERSATION_HISTORY_LIMIT=10

# Database Configuration
# Path to SQLite database file (default: telecust.db in current directory)
# For Docker: /data/telecust.db
# DB_PATH=telecust.db

# Server Configuration
# HTTP server port (default: 8080)
PORT=8080
